# rc-vendor-provisioning-mcp

Auto-generated by the Minimal-MCP-Architect for Rocket.Chat.

## Overview
This MCP server provides a deterministic, AI-native interface to the Rocket.Chat API. It utilizes TOON (Token-Oriented Object Notation) to minimize LLM context bloat and includes built-in payload truncation to protect agentic loops.

## Available Tools (7)
The following tools were autonomously selected and scaffolded:

* **`post_api_v1_users_create`**
  * *Shorthand:* `users.new(name:o, email:o, password:o, u:o, [active:o], [nickname:o], [bio:o], [joinDefaultChannels:o], [statusText:o], [roles:o], [requirePasswordChange:o], [setRandomPassword:o], [sendWelcomeEmail:o], [verified:o], [customFields:o])`
* **`post_api_v1_users_update`**
  * *Shorthand:* `users.upd(uid:o, data:o)`
* **`post_api_v1_channels_invite`**
  * *Shorthand:* `channels.invite()`
* **`post_api_v1_groups_invite`**
  * *Shorthand:* `groups.invite(rid:o, uid:o)`
* **`post_api_v1_im_create`**
  * *Shorthand:* `dm.new()`
* **`post_api_v1_chat_postMessage`**
  * *Shorthand:* `chat.post()`
* **`get_api_v1_users_info`**
  * *Shorthand:* `users.get()`

## Getting Started

1. **Install Dependencies:**
   ```bash
   npm install
   ```

2. **Configure Environment:**
   Rename `.env.example` to `.env` and add your Rocket.Chat credentials:
   ```
   ROCKETCHAT_URL=http://localhost:3000
   ROCKETCHAT_AUTH_TOKEN=your_personal_access_token
   ROCKETCHAT_USER_ID=your_user_id
   ```

3. **Build and Test:**
   ```bash
   npm run build
   npx @modelcontextprotocol/inspector node --env-file=.env dist/server.js
   ```
