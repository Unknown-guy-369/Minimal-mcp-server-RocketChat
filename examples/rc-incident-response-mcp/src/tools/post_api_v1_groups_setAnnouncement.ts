/**
 * Tool: post_api_v1_groups_setAnnouncement
 * Generated by Minimal-MCP-Architect
 */
import { rcClient } from "../rc-client.js";
import { z } from "zod";

export const tool = {
  name: "post_api_v1_groups_setAnnouncement",
  description: "groups.setAnn(rid:o, announcement:o)", //  The TOON Context Saver
  inputSchema: z.object({
    roomId: z.string(),
    announcement: z.string(),
  }),
  handler: async (args: any) => {
    let resolvedPath = "/api/v1/groups.setAnnouncement"; // e.g., "/api/v1/settings/{_id}"
    
    // Dynamically replace path variables
    if (args.roomId !== undefined) {
       const value = encodeURIComponent(String(args.roomId));
       // Replace the bracketed version `{paramName}`
       resolvedPath = resolvedPath.replace("{" + "roomId" + "}", value);
       // Fallback: replace the raw string just in case
       resolvedPath = resolvedPath.replace("roomId", value);
    }
    if (args.announcement !== undefined) {
       const value = encodeURIComponent(String(args.announcement));
       // Replace the bracketed version `{paramName}`
       resolvedPath = resolvedPath.replace("{" + "announcement" + "}", value);
       // Fallback: replace the raw string just in case
       resolvedPath = resolvedPath.replace("announcement", value);
    }

    return await rcClient.request("POST", resolvedPath, { body: args });
  }
};
