# rc-channel-archival-mcp

> Auto-generated by the **Minimal-MCP-Architect** for Rocket.Chat.

## Overview

This MCP server provides a deterministic, AI-native interface to the Rocket.Chat API.
It utilizes **TOON** (Token-Oriented Object Notation) to minimize LLM context bloat
and includes built-in **8KB payload truncation** and **429 exponential backoff** to
protect agentic loops.

---


## 📦 Project Details

| Field | Value |
|-------|-------|
| **Location** | `` |
| **Total Tools** | 8 |

### Primary Tools

| Tool | TOON Shorthand |
|------|----------------|
| `post_api_v1_rooms_export` | `rooms.export(rid:o, type:o, [dateFrom:o], [dateTo:o], [format:o], [toUsers:o], [toEmails:o], [messages:o], [subject:o])` |
| `get_api_v1_channels_members` | `channels.members()` |
| `post_api_v1_channels_kick` | `channels.kick(rid:o, uid:o)` |
| `post_api_v1_channels_archive` | `channels.archive(rid:o)` |
| `get_api_v1_groups_members` | `groups.members()` |
| `post_api_v1_groups_kick` | `groups.kick(rid:o, uid:o)` |
| `post_api_v1_groups_archive` | `groups.archive(rid:o)` |
| `post_api_v1_chat_postMessage` | `chat.post()` |

---

## 🛠️ Getting Started

### 1. Install Dependencies

```bash
cd 
npm install
```

### 2. Configure Environment

Rename `.env.example` to `.env` and set your Rocket.Chat credentials:

```env
ROCKETCHAT_URL=http://localhost:3000
ROCKETCHAT_AUTH_TOKEN=your_personal_access_token
ROCKETCHAT_USER_ID=your_user_id
```

### 3. Build & Run

```bash
npm run build
npm start
```

### 4. Inspect with MCP Inspector

```bash
npx @modelcontextprotocol/inspector node --env-file=.env dist/server.js
```

---

## 🧪 Testing

Every tool includes a corresponding Vitest suite for zero-regression safety:

```bash
npm run test
```

**Generated Test Files:**
- `tests/post_api_v1_rooms_export.test.ts`
- `tests/get_api_v1_channels_members.test.ts`
- `tests/post_api_v1_channels_kick.test.ts`
- `tests/post_api_v1_channels_archive.test.ts`
- `tests/get_api_v1_groups_members.test.ts`
- `tests/post_api_v1_groups_kick.test.ts`
- `tests/post_api_v1_groups_archive.test.ts`
- `tests/post_api_v1_chat_postMessage.test.ts`

---

## Architecture

```
rc-channel-archival-mcp/
├── src/
│   ├── server.ts          # MCP entry point with dynamic tool registration
│   ├── rc-client.ts       # Axios wrapper (8KB truncation, 429 backoff)
│   └── tools/             # One file per tool
│       ├── post_api_v1_rooms_export.ts
│       ├── get_api_v1_channels_members.ts
│       ├── post_api_v1_channels_kick.ts
│       ├── post_api_v1_channels_archive.ts
│       ├── get_api_v1_groups_members.ts
│       ├── post_api_v1_groups_kick.ts
│       ├── post_api_v1_groups_archive.ts
│       ├── post_api_v1_chat_postMessage.ts
├── tests/                 # Vitest suites
│   ├── setup.ts
│   ├── post_api_v1_rooms_export.test.ts
│   ├── get_api_v1_channels_members.test.ts
│   ├── post_api_v1_channels_kick.test.ts
│   ├── post_api_v1_channels_archive.test.ts
│   ├── get_api_v1_groups_members.test.ts
│   ├── post_api_v1_groups_kick.test.ts
│   ├── post_api_v1_groups_archive.test.ts
│   ├── post_api_v1_chat_postMessage.test.ts
├── .env.example
├── tsconfig.json
├── vitest.config.ts
└── package.json
```

---

<sub>Generated by RC-Master-Architect v1.0.0 — powered by TOON compression.</sub>
