# rc-omnichannel-router-mcp

Auto-generated by the Minimal-MCP-Architect for Rocket.Chat.

## Overview
This MCP server provides a deterministic, AI-native interface to the Rocket.Chat API. It utilizes TOON (Token-Oriented Object Notation) to minimize LLM context bloat and includes built-in payload truncation to protect agentic loops.

## Available Tools (5)
The following tools were autonomously selected and scaffolded:

* **`get_api_v1_livechat_department`**
  * *Shorthand:* `livechat/department()`
* **`post_api_v1_livechat_room_forward`**
  * *Shorthand:* `livechat/room.forward(rid:o, [uid:o], [departmentId:o])`
* **`post_api_v1_livechat_inquiries_take`**
  * *Shorthand:* `livechat/inquiries.take(inquiryId:o, [uid:o])`
* **`get_api_v1_canned_responses`**
  * *Shorthand:* `canned-responses()`
* **`post_api_v1_livechat_message`**
  * *Shorthand:* `livechat/message(token:o, rid:o, msg:o, [_id:o], [agent:o])`

## Getting Started

1. **Install Dependencies:**
   ```bash
   npm install
   ```

2. **Configure Environment:**
   Rename `.env.example` to `.env` and add your Rocket.Chat credentials:
   ```
   ROCKETCHAT_URL=http://localhost:3000
   ROCKETCHAT_AUTH_TOKEN=your_personal_access_token
   ROCKETCHAT_USER_ID=your_user_id
   ```

3. **Build and Test:**
   ```bash
   npm run build
   npx @modelcontextprotocol/inspector node --env-file=.env dist/server.js
   ```
