# rc-cleanup-bot

Auto-generated by the Minimal-MCP-Architect for Rocket.Chat.

## Overview
This MCP server provides a deterministic, AI-native interface to the Rocket.Chat API. It utilizes TOON (Token-Oriented Object Notation) to minimize LLM context bloat and includes built-in payload truncation to protect agentic loops.

## Available Tools (10)
The following tools were autonomously selected and scaffolded:

* **`post_api_v1_chat_delete`**
  * *Shorthand:* `chat.del(rid:o, mid:o, [asUser:o])`
* **`post_api_v1_channels_delete`**
  * *Shorthand:* `channels.del([rid:o], [roomName:o])`
* **`post_api_v1_groups_delete`**
  * *Shorthand:* `groups.del([rid:o], [roomName:o])`
* **`post_api_v1_channels_kick`**
  * *Shorthand:* `channels.kick(rid:o, uid:o)`
* **`post_api_v1_groups_kick`**
  * *Shorthand:* `groups.kick(rid:o, uid:o)`
* **`post_api_v1_integrations_remove`**
  * *Shorthand:* `integrations.remove(integrationId:o, type:o)`
* **`get_api_v1_rooms_cleanHistory`**
  * *Shorthand:* `rooms.cleanHistory(rid:o, latest:o, oldest:o, [inclusive:o], [excludePinned:o], [filesOnly:o], [users:o], [limit:o], [ignoreDiscussion:o], [ignoreThreads:o])`
* **`get_api_v1_channels_list`**
  * *Shorthand:* `channels.ls()`
* **`get_api_v1_groups_list`**
  * *Shorthand:* `groups.ls()`
* **`get_api_v1_integrations_list`**
  * *Shorthand:* `integrations.ls()`

## Getting Started

1. **Install Dependencies:**
   ```bash
   npm install
   ```

2. **Configure Environment:**
   Rename `.env.example` to `.env` and add your Rocket.Chat credentials:
   ```
   ROCKETCHAT_URL=http://localhost:3000
   ROCKETCHAT_AUTH_TOKEN=your_personal_access_token
   ROCKETCHAT_USER_ID=your_user_id
   ```

3. **Build and Test:**
   ```bash
   npm run build
   npx @modelcontextprotocol/inspector node --env-file=.env dist/server.js
   ```
